# =============================================================
# ObsiRAG — Configuration
# Copier ce fichier en .env et adapter les valeurs
# =============================================================

# ---- Coffre Obsidian ----------------------------------------
# Chemin ABSOLU vers votre coffre sur l'hôte
VAULT_PATH=/path/to/your/obsidian/vault

# Répertoire des données système ObsiRAG (ChromaDB, index, stats, queries)
# Doit être HORS du coffre Obsidian pour éviter la sync iCloud
# Exemple : APP_DATA_DIR=/Users/ton_user/Library/Application Support/ObsiRAG
APP_DATA_DIR=./data

# ---- Ollama -----------------------------------------------
# Ollama     → http://localhost:11434/v1
OLLAMA_BASE_URL=http://localhost:11434/v1

# Nom exact du modèle de chat chargé dans Ollama (ollama pull <modèle>)
OLLAMA_CHAT_MODEL=gemma3:4b

# (Recommandé) Modèle d'embedding via Ollama — exploite le Metal/ANE de l'hôte
# et libère le CPU (~80% → ~0%). Nécessite d'avoir pulé le modèle : ollama pull nomic-embed-text
# Après avoir défini cette valeur, supprimer data/chroma et relancer une indexation complète
# (la dimension des vecteurs change selon le modèle).
#
# Recommandé : nomic-embed-text (768 dimensions, multilingue)
# Laisser vide = fallback sentence-transformers CPU
OLLAMA_EMBED_MODEL=nomic-embed-text

# ---- Embeddings locaux (sentence-transformers) --------------
# Modèle multilingue — supporte le français nativement
EMBEDDING_MODEL=paraphrase-multilingual-MiniLM-L12-v2

# ---- Logging ------------------------------------------------
LOG_LEVEL=INFO

# ---- Auto-apprentissage ------------------------------------
AUTOLEARN_ENABLED=true

# Intervalle entre chaque cycle (minutes)
AUTOLEARN_INTERVAL_MINUTES=60

# Nombre maximum de notes traitées par cycle
AUTOLEARN_MAX_NOTES_PER_RUN=5

# Fenêtre de détection des nouvelles notes (heures)
AUTOLEARN_LOOKBACK_HOURS=24

# ---- RAG ----------------------------------------------------
# Taille des chunks (en mots)
CHUNK_SIZE_WORDS=350

# Chevauchement entre chunks (en mots)
CHUNK_OVERLAP_WORDS=50

# Nombre de chunks retournés par la recherche sémantique
SEARCH_TOP_K=10

# Nombre maximum de chunks injectés dans le contexte LLM
MAX_CONTEXT_CHUNKS=12

# ---- WUDD.ai — Entités NER officielle ------------------
# URL de base du serveur WUDD.ai (sans slash final)
# Ex: http://localhost:5050
WUDDAI_ENTITIES_URL=http://localhost:5050
