# Interface utilisateur
streamlit>=1.35.0

# Base de données vectorielle
chromadb>=0.5.3

# Embeddings locaux multilingues
sentence-transformers>=3.1.0

# Pin transformers < 4.52 — bug NameError: 'nn' not defined introduit dans 4.52.0
transformers>=4.41.0,<4.52.0

# Dépendance du backend d'embeddings OpenAI-compatible utilisé quand Ollama est activé pour les embeddings
openai>=1.50.0

# Génération locale Apple Silicon (remplace Ollama pour le LLM de chat)
mlx-lm>=0.31.0

# Surveillance du système de fichiers
watchdog>=4.0.0

# Recherche web (enrichissement quand info absente du coffre)
duckduckgo-search>=6.0.0

# Planificateur de tâches de fond
APScheduler>=3.10.4

# Logging professionnel
loguru>=0.7.2

# Gestion de la configuration
pydantic-settings>=2.5.0

# Parsing Markdown / frontmatter YAML
python-frontmatter>=1.1.0

# Graphe de connaissances
networkx>=3.3
pyvis>=0.3.2

# Visualisation & données
plotly>=5.24.0
pandas>=2.2.0

# Utilitaires dates
python-dateutil>=2.9.0

# NER — extraction d'entités nommées (modèle multilingue chargé dans Dockerfile)
spacy>=3.7.0

# Recherche web (auto-learner)
ddgs>=0.1.0
